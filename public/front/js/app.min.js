$(function(){
  $('#menu').metisMenu().show();

var hero_s = $(".banner");
hero_s.owlCarousel({
    loop: true,
    margin: 0,
    nav: true,
    items: 1,
    dots: false,
    animateIn: 'fadeIn',
    animateOut: 'fadeOut',
    navText: ['<i class="icofont-bubble-left " style="font-size: 35px"></i>', '<i class="icofont-bubble-right" style="font-size: 35px"></i>'],
    smartSpeed: 5200,
    autoHeight: false,
    autoplay: true
});

$(".special-slider , .latest-slider").owlCarousel({
    loop:true,
    items: 5,
    dots:false,
    navText: ['<i class="icofont-thin-left " style="font-size: 70px"></i>', '<i class="icofont-thin-right" style="font-size: 70px"></i>'],
    smartSpeed: 1200,
    autoHeight: false,
    autoplay: true,
    nav:true,
    autoplayHoverPause:true,
    responsive:{
        300:{
            items:1
        },
        375:{
            items:2
        },
        600:{
            items:2
        },
        1000:{
            items:3
        },
        1328:{
            items:4
        },
        1400:{
            items:4
        }
    }

});

$(".slick-slide").owlCarousel({
    loop:true,
    items: 5,
    dots:false,
    navText: ['<i class="icofont-thin-left " style="font-size: 70px"></i>', '<i class="icofont-thin-right" style="font-size: 70px"></i>'],
    smartSpeed: 1200,
    autoHeight: false,
    autoplay: true,
    nav:true,
    margin:10,
    autoplayHoverPause:true,
    responsive:{
        300:{
            items:1
        },
        375:{
            items:2
        },
        600:{
            items:3
        },
        1000:{
            items:3
        },
        1328:{
            items:4
        },
        1400:{
            items:4
        }
    }

});




// 'use strict';

$(window).on('load', function() {
    /*------------------
        Preloder
    --------------------*/
    $(".loader").fadeOut();
    $("#preloder").delay(400).fadeOut("slow");

});


//jQuery for hide and show the comments in review section
$(function() {
    $('article.reviews').slice(0,3).show();
    $('.show-more').on('click',function(){
        $('article:hidden').slice(0,3).slideDown();
        if($('article:hidden').length === 0){
            $('.show-more').fadeOut('slow');
        }
    });
});



///* calling script form Xzoom */(function ($) {
(function ($) {
    $(document).ready(function() {
        $('.xzoom, .xzoom-gallery').xzoom({zoomWidth: 500, lensShape: 'circle', title: true, tint: '#333', Xoffset: 15});

        //Integration with hammer.js
        var isTouchSupported = 'ontouchstart' in window;

        if (isTouchSupported) {
            //If touch device
            $('.xzoom').each(function(){
                var xzoom = $(this).data('xzoom');
                xzoom.eventunbind();
            });

            $('.xzoom').each(function() {
                var xzoom = $(this).data('xzoom');
                $(this).hammer().on("tap", function(event) {
                    event.pageX = event.gesture.center.pageX;
                    event.pageY = event.gesture.center.pageY;
                    var s = 1, ls;

                    xzoom.eventmove = function(element) {
                        element.hammer().on('drag', function(event) {
                            event.pageX = event.gesture.center.pageX;
                            event.pageY = event.gesture.center.pageY;
                            xzoom.movezoom(event);
                            event.gesture.preventDefault();
                        });
                    };

                    xzoom.eventleave = function(element) {
                        element.hammer().on('tap', function(event) {
                            xzoom.closezoom();
                        });
                    };
                    xzoom.openzoom(event);
                });
            });

        } else {
            //If not touch device

        }
    });
})(jQuery);


//jQuery time
var current_fs, next_fs, previous_fs; //fieldsets
var left, opacity, scale; //fieldset properties which we will animate
var animating; //flag to prevent quick multi-click glitches

$(".next").click(function(){

    current_fs = $(this).parents('fieldset');
    next_fs=$(this).parents('fieldset').next();

    //activate next step on progressbar using the index of next_fs
    $("#progressbar li").eq($("fieldset").index(next_fs)).addClass("actively");

    //show the next fieldset
    next_fs.show();
    current_fs.hide();

});

$(".submit").click(function(){
    return false;
});

$('.terms-and-conditions-link').on('click',function () {
    $('.termscondition').fadeToggle();
})


function accountsettings(evt, settingName) {
    // Declare all variables
    var i, tabcontent, tablinks;

    // Get all elements with class="tabcontent" and hide them
    tabcontent = document.getElementsByClassName("tabcontent");
    for (i = 0; i < tabcontent.length; i++) {
        tabcontent[i].style.display = "none";
    }

    // Get all elements with class="tablinks" and remove the class "active"
    tablinks = document.getElementsByClassName("tabslinks");
    for (i = 0; i < tablinks.length; i++) {
        tablinks[i].className = tablinks[i].className.replace(" active-tab", "");
    }

    // Show the current tab, and add an "active" class to the link that opened the tab
    document.getElementById(settingName).style.display = "block";
    evt.currentTarget.className += " active-tab";


}
// Get the element with id="defaultOpen" and click on it
document.getElementById("defaultOpen").click();

  
})

